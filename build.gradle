plugins {
    id 'java'
}

version = '1.0.0'
group = 'com.clatix'

repositories {
    mavenCentral()
}

jar {
    manifest {
        attributes(
            'Main-Class': 'com.clatix.ClatixClientMod'
        )
    }
    archiveFileName = 'clatix-client-1.0.0.jar'
}